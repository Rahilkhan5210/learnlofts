services:
  - type: web
    name: learnlofts-backend
    env: node
    buildCommand: cd server && npm install
    startCommand: cd server && npm start
    rootDir: .
    envVars:
      - key: NODE_ENV
        value: production 